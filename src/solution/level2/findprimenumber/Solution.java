package solution.level2.findprimenumber;

import java.util.ArrayList;

public class Solution {
    public int solution(String numbers) {
        int answer = 0;
        ArrayList<Integer> checked = new ArrayList<>();
        int max = numbers.length();
        int length = 1;
        while (length <= max) {

        }

        return answer;
    }
}